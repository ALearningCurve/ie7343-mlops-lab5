# Terraform resources for lab5

This folder creates baseline GCP resources for this lab:

- Required APIs
- One GCS bucket for data and model artifacts
- One service account for Workflow calls
- Cloud Functions for each step of the workflow

For usage, see the README.md in the repo root.